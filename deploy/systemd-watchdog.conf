# Hardware watchdog: if the system hard-hangs (e.g. a driver deadlock), the bcm2835
# watchdog reboots it automatically instead of requiring a manual power-cycle.
[Manager]
RuntimeWatchdogSec=15s
RebootWatchdogSec=2min
